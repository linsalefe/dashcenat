DATABASE_URL=postgresql+asyncpg://dashcenat:dashcenat_dev@localhost:5442/dashcenat
JWT_SECRET=trocar-em-producao
JWT_EXPIRE_MIN=480
# Fernet key (gere com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FERNET_KEY=
# Doity (API pública de inscrições — token por evento, cifrado em core.eventos)
DOITY_BASE_URL=https://api.doity.com.br/public/v1
